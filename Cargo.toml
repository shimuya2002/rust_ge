[package]
name = "rust_ge"
version = "0.1.0"
edition = "2024"

[dependencies]
num_enum="*"
[build-dependencies]
bindgen = "*"

[features]
default=["non_bindings","use_sdl3"]
non_bindings=[]
use_sdl3=[]
use_sdl2=[]